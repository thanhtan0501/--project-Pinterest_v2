const routes = {
    all: "*",
    home: "/",
    profile: "/:username",
    search: "/search/pins",
    setting_profile: "/settings/profile",
    setting_account: "/settings/account-settings",
    pinDetail: "/pin/:id",
    createPin: "/create-pin",
};

export default routes;
