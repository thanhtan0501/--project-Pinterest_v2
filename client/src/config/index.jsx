import { userMenuNav, guestMenuNav } from "./menuNav";
import * as pinModalOption from "./modal";
import routes from "./routes";
const config = {
    routes,
    userMenuNav,
    guestMenuNav,
    pinModalOption,
};
export default config;
